export interface SecondTableProps {
  name: string;
  date: string;
  id: string;
}

export interface FirstTableDataProps {
  name: string;
  date: string;
  id: string;
  workflow: string;
  stage: string;
}
